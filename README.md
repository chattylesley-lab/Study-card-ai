# StudyCard — no API key version

Upload a PDF, DOCX, TXT, or Markdown file. The app reads the document, ranks important material, creates main ideas, flashcards, and a multiple-choice quiz.

This version does not require an OpenAI API key.

## Streamlit Community Cloud
Deploy `app.py` from the repository. Streamlit installs the packages in `requirements.txt` automatically.
